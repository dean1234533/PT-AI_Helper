# The public app homepage lives at the root. The installed PWA launches at
# /dashboard from its manifest. Its service worker deliberately does not cache
# HTML navigations, so this homepage can never be mistaken for the SPA shell.
/ /app-home 200
/pricing /app-home 200

# BrowserRouter routes are handled by the SPA fallback. Real assets and Pages
# Functions take precedence.
/* /index.html 200
